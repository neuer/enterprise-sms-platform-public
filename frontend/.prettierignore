dist
node_modules
coverage
package-lock.json

# 静态直出资源（独立测试页等）不参与应用构建，保持手写原样
public

# 采纳期：手写源码/测试与 Prettier 规范化差距大（实测 printWidth=120 全量约 8.5k 行漂移，
# 主因是 .vue 模板刻意长行排版与手写参数换行风格），先只对基建与配置文件启用，
# src/ 与 tests/ 后续按目录渐进收编，收编一块删一行。
src
tests
