# Copy to .env.test-update. The local file is ignored by Git.
# Only these three keys are accepted; never add passwords or private keys.
SMS_TEST_UPDATE_TARGET=operator@test-host
SMS_TEST_UPDATE_PORT=22
# Non-secret HTTPS origin used by the development vendor-test marker.
SMS_VENDOR_LIVE_TEST_ORIGIN=https://vendor.example.invalid
