# 默认部署契约：Web Nginx 是最外层可信代理，不接受任何客户端代理头。
# 生产外部 TLS 终结拓扑必须由 deploy/scripts/render_trusted_proxy_conf.py
# 在 sms-compose up 前覆盖本文件，写入精确 set_real_ip_from 与 geo 信任项。
geo $realip_remote_addr $sms_trusted_proxy {
    default 0;
}
