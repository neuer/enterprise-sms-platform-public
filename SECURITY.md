# 安全策略

本文件同时定义本仓库的漏洞私密报告要求，以及维护者、贡献者和自动化安全审查（包括 Codex Security）应采用的仓库级安全边界。

本文件不是对任何线上、测试或第三方系统进行主动探测、利用或破坏性验证的授权。所有动态验证必须限定在明确获准的本地或隔离环境中。

## 支持范围

仅当前默认分支 `main` 和最新发布候选接受安全修复。历史提交、未合并分支、测试证据以及未进入受支持发布物的示例代码，不构成受支持版本。

安全问题应针对可识别的提交、发布候选或部署合同报告。维护者未明确承诺为历史版本回移安全修复。

## 系统与评审范围

本仓库实现企业短信管理平台，作为内部业务系统、平台操作人员与短信厂商网关之间的统一治理通道。安全评审范围包括：

- FastAPI API、Web 认证与管理接口；
- 本地账号与 AD/LDAP Provider、JWT 会话、角色和部门授权；
- 应用 API Key、来源 IP、类别、配额、限流与频控；
- 短信受理、审批、模板、黑名单、幂等、状态机和发送流水线；
- Celery worker、事务性 Outbox、调度、对账及故障恢复；
- 短信厂商适配、状态报告与上行回复拉取；
- 应用结果回调、告警与受控出站访问；
- 手机号、短信内容、OTP、导入、导出和原始厂商载荷的保护；
- PostgreSQL、Redis、数据库迁移、运行角色和审计日志；
- Vue 前端、Nginx、可信代理、外部 TLS 终结与 Docker Compose 部署合同；
- GitHub Actions、发布门禁、公开仓库边界和供应链流程。

`PRD.md` 描述产品行为，`AGENTS.md` 描述工程不变量，`openapi.yaml` 和 `schema.sql` 分别约束接口与数据，`deploy/docker-compose.yml` 是部署合同。本文件定义安全边界、报告条件和严重度语境。上述证据发生冲突时，不得把任一文档自动当作抑制漏洞的依据；应根据当前源码和实际部署合同记录冲突并要求责任人裁决。

## 关键资产

重点保护的资产包括：

- 短信厂商凭据、数据加密与 HMAC 密钥、审计上下文签名密钥、JWT 密钥、LDAP Bind 凭据、数据库与 Redis 凭据、回调 Secret、应用 API Key 和邮件/告警投递凭据；
- 手机号、短信正文、验证码、上行回复、状态报告、导入文件、导出文件和客户回调载荷；
- 用户主体、外部身份、角色、部门、应用归属、会话状态和安全版本；
- 短信发送权限、审批结论、配额、频控、幂等事实、状态机和对账结果；
- 审计记录、关联 ID、安全日志、发布提交、构建产物和部署配置。

## 威胁模型与信任边界

### 可能的攻击者

安全评审应考虑下列主体在其现有权限范围内尝试扩大能力：

- 未认证网络客户端；
- 已认证的查看员、操作员、审批员或管理员；
- 持有某一应用 API Key 的内部接入方；
- 被入侵、误配置或行为异常的 LDAP、短信厂商、回调接收端、邮件或告警服务；
- 仅获得某一 worker、数据库运行角色、Redis ACL 身份或容器权限的攻击者；
- 提交 Pull Request、修改工作流或影响构建输入的贡献者及 fork；
- 能控制导入文件、模板变量、短信内容、URL、HTTP 头、厂商响应、队列消息或配置字段的调用者。

### 信任假设

- 自托管运维人员负责主机、网络、防火墙、外部 TLS 终结器、Secret 权威源和批准的出站目标；但“需要运维配置”不是绕过安全默认值、失败关闭门禁或最小权限的免责理由。
- 管理员可执行文档明确授权的管理动作，但无权读取明文运行 Secret、绕过审计、扩大部署侧 LDAP/告警/厂商出站边界、伪造身份事实或破坏本文件中的数据与发送不变量。
- 外部请求、导入内容、厂商响应、LDAP 属性、回调端点及其响应均视为不可信数据，除非由明确的协议校验和部署侧允许列表约束。
- PostgreSQL 是业务状态、幂等、配额、频控和发送副作用的权威事实源。Redis 仅承担 broker、认证状态或可重建投影职责；Redis 缺失或不确定不得被解释为“零使用量”“未发送”或“允许继续”。
- 不同容器、worker、数据库角色、Redis 故障域、Web 用户、应用和部门之间均是需要验证的权限边界。

## 必须保持的安全不变量

### 1. 认证、会话与授权

- Web 登录必须显式选择认证 Provider，禁止失败后自动回退、隐藏注册或隐藏提权路径。
- 授权必须基于服务端权威的账号、身份、Provider、角色、部门、应用归属和安全版本；前端状态、JWT 内的陈旧快照或请求参数不得成为最终授权事实源。
- 所有资源读取和变更必须在解析最终资源身份后校验角色、部门、所有权和操作权限；认证成功不能替代授权。
- 新签发的 Refresh Token 只能位于受限路径的 `HttpOnly` Cookie 中，生产环境必须使用 `Secure`，刷新和注销必须执行规范化的同源校验。任何历史请求体兼容读取都必须是有限、可观测且有明确退出条件的迁移路径。新代码不得把 Access Token、首次改密令牌或 step-up 令牌写入 `localStorage`、`sessionStorage`、IndexedDB、URL、日志或持久 DOM 状态；历史 Web Storage 只读迁移路径必须在读取后立即清理。
- Refresh 单次轮换保留 5 秒、不可递归延长的 grace：Lua 状态机为首次轮换 `{1}`、grace 内同一旧 binding 重试 `{2}`、family 缺失 `{0}`、窗口外或跨代 replay `{-1}`。`{1}/{2}` 不得吊销刚生成的 family；`{-1}/{0}` 必须 fail closed。grace 记录不得保存 token 明文，replacement 只能按前序 token 确定性重建。浏览器可用 Web Locks 做跨标签页单飞，但不得用 storage/`BroadcastChannel` 传递凭据；BFCache 恢复必须向服务端重新验证，失败即清除全部标签页。
- 账号、身份、角色、Provider、密码或强制下线状态改变后，旧会话必须按安全版本和服务端状态失效；认证依赖不可用时必须失败关闭。
- API Key 明文只能在受控创建或轮换时一次性返回，持久化时只能保存不可逆摘要。类别、配额、速率、来源 IP 和轮换宽限必须绑定到正确应用，并使用统一的可信客户端 IP。
- 审批人不得审批本人提交；任何角色绕过、跨部门访问、跨应用访问或最后有效管理员保护失效均应视为安全问题。

### 2. 不可逆短信副作用

- 所有真实短信发送必须经过统一受理流水线、策略检查和厂商适配层；业务代码不得绕过审批、黑名单、类别策略、配额、频控、计费或厂商调用边界。
- 所有可产生短信副作用的外部入口必须绑定稳定主体、幂等键和规范化请求指纹。同一键与同一请求只能返回原结果；同一键与不同请求必须冲突，不能静默复用。
- PostgreSQL 幂等事实的保留期必须覆盖定时发送和完整副作用生命周期。Redis 只能加速协调，不能成为唯一幂等事实。
- 厂商请求超时、连接异常、响应超限、协议异常或其他无法确认厂商是否已接收请求的情况必须进入 `uncertain` 或等价的“结果未知”状态，严禁自动重发。只有受控对账流程能够解除该状态。
- 队列重投、Outbox、租约、fencing、重试和人工恢复不得造成重复发送、重复配额回补、重复频控释放或非法状态迁移。
- 配额、频控、审批、黑名单、营销合规、真实联调预算和控制状态不可确认时必须失败关闭，不能把缺失数据当作允许条件。

### 3. 个人信息与敏感内容

- 手机号不得以明文持久化到数据库、JSONB、文件、缓存、队列、日志、指标、审计或错误响应。逐号码持久化必须保持 `phone_enc`、`phone_hmac`、`phone_mask` 和 `key_version` 的绑定关系。
- 手机号明文仅可在真实厂商调用、授权详情查看、授权导出、回调投递、原始载荷解析或迁移等明确边界内短暂存在于有界内存中；不得再次写回任一持久层。
- 验证码等敏感内容必须在持久化、日志、审计、回调和展示边界按产品合同打码；计费和真实发送可在受控内存中使用发送原文。
- 厂商 GetReport/GetReply 等“拉取即消费”响应必须先完整加密落库，再进行业务解析；完整捕获的解析失败必须保留可重放的密文事实。截断捕获必须标记为不可自动重放，完整但超过自动解析上限的捕获仅允许人工重放。
- 导入和导出中间文件必须使用带对象身份、顺序和终止完整性语义的认证加密格式，路径、大小和权限必须受控；手机号或其他敏感明文不得写入临时磁盘。
- CSV/XLSX 等导出必须防止电子表格公式注入，并保持普通文本、日期和数值语义。

### 4. 加密与 Secret

- 新增敏感持久化必须使用带明确业务上下文的 AES-GCM 认证加密，并绑定数据域、表、列、对象身份和密钥版本。普通业务读取默认不得降级到旧版通用 AAD；兼容读取只能存在于白名单迁移任务中，并具备审计、指标和退出条件。
- 密钥轮换必须保留明确版本，AES 与 HMAC keyring 必须一致；未知版本、错误上下文、密文移动、截断或篡改必须失败。
- 运行凭据默认只能从文件型 Secret 读取，不得进入 Git、镜像、环境变量明文、命令参数、日志、指标、审计或普通 API 响应。
- `AGENTS.md` 中已经明确的少数产品例外必须保持专用、最小和不可复用：安全日报投递凭据只能用于独立 mailer 配置；真实厂商联调凭据只能短暂存在于浏览器易失内存，并通过受控加密边界交给专用 agent。任何扩大这些例外的行为都需要独立安全评审。
- 管理界面不得把“Secret 已配置”转化为读取、摘要、前缀、长度、哈希或跨目标复用 Secret 的能力。
- 管理员配置的企微 webhook 是可重放凭据：API 只能使用独立 X25519 公钥写入认证信封，只有承担实际投递的 callback worker 可挂载私钥并解封；旧信封、错误密钥或篡改必须失败关闭。

### 5. 出站访问、LDAP 与回调

- 生产厂商访问必须经唯一厂商适配层使用 HTTPS、固定协议、绝对总超时、响应头和正文硬上限，并禁止继承宿主环境代理。厂商错误不得把凭据、手机号或完整响应写入日志。
- LDAP 只能访问部署侧批准的精确 `ldaps://host:port` 或凭据 Profile。保存、测试、激活和实际认证必须使用同一目标校验；UI/API 不能扩大允许列表，也不能让 Bind Secret 发送到动态或未批准目标。
- 应用回调 URL 必须在保存和每次出站前重新验证协议、主机、解析地址和内网/保留地址策略；禁止重定向绕过、DNS 重绑定、环境代理改道和无限响应。
- 回调必须在最终原始请求体上使用时间戳与 HMAC 签名，并携带稳定事件 ID。回调 Secret 只能以认证密文保存；重试、租约和 fencing 不得造成同一事件被错误提交为多个独立事实。
- 告警、邮件和其他可配置网络目标必须受部署侧允许列表和最小权限凭据约束；管理员配置不能扩大基础设施批准的出站范围。

### 6. 审计与可追责性

- 所有安全相关写操作以及手机号解密、导出、真实联调、Secret 轮换等敏感读取或动作必须生成可关联的审计事实。
- 需要原子可追责性的业务变更与审计写入必须位于同一事务；审计失败时不得返回敏感数据或提交无审计的业务变更。
- 审计必须记录稳定主体、身份或应用、角色、可信客户端 IP、动作、对象和当前 correlation ID，但不得包含手机号、手机号 HMAC、手机号密文、短信正文、密码、Token、Secret、完整请求体或完整异常载荷。
- 数据库自定义 GUC 不是身份权威。业务角色提交的稳定审计主体必须以独立密钥绑定事务 ID、`session_user`、correlation ID 和主体字段；运行数据库角色不得读取验证表，缺失、跨事务重放或签名不匹配必须拒绝业务写入。
- 运行角色不得拥有 `audit_log` 的 `UPDATE`、`DELETE`、`TRUNCATE`、表所有权或超级用户能力。应用代码中修改或删除审计事实属于缺陷。

### 7. 网络、代理与部署

- 生产公开流量只能经批准的外部 TLS 终结器进入。内部 Nginx HTTP 上游、API、数据库、Redis 和 Mock 服务不得直接暴露给不可信网络。
- 外部 TLS 入口必须使用 TLS 1.2 或更高版本、正确证书、HTTP 到规范 HTTPS origin 的受控跳转和 HSTS；内部 HTTP 响应不得伪造 HSTS。
- 只有明确批准的上一跳代理可以声明客户端 IP 和外部协议。客户端自带的 `Forwarded`、`X-Forwarded-*` 或 `X-Real-IP` 不得影响 IP 白名单、登录限流、审计、Cookie `Secure` 或同源判断。
- 客户端 IP 和外部 origin 必须各自由统一 helper 产生；代理链无法解析或配置不一致时必须失败关闭或使用明确的不可信哨兵，不能回退到用户可控字符串。
- 长期运行容器必须保持非 root、只读根文件系统、`cap_drop: ALL`、`no-new-privileges`、资源上限和最小可写目录。不同职责必须使用独立数据库角色、Redis ACL 身份和 Secret 挂载。
- `/readyz` 或等价接流门禁必须验证必要 Secret、数据库迁移、分区、关键配置和依赖故障域；不可确认时不得通过放宽超时、跳过检查或扩大权限强行接流。

### 8. 公开仓库与供应链

- 本公开仓库不得引入私有历史、私有 Git 对象、真实 `.env`、运行 Secret、客户数据、日志、导入/导出文件、内部地址或其他受限证据。
- GitHub Actions 默认使用只读权限；fork 工作流不得取得环境 Secret 或写权限。任何工作流、发布或自动合并逻辑不得绕过 required checks、required reviews、受保护分支或精确提交绑定。
- 发布候选、镜像和制品必须可追溯到经过审查并通过门禁的受保护 `main` 提交；标签、构建输入或产物替换不得破坏该绑定。
- 至少下列安全域属于安全敏感边界，并应按 `.github/CODEOWNERS` 和仓库 ruleset 进行独立评审；分类来源是 `deploy/scripts/protected_path_policy.py`，域内新文件默认 FULL：
  - `backend/app/core/`
  - `backend/app/api/`
  - `backend/app/services/`
  - `backend/app/tasks/`
  - `backend/app/vendor/`
  - `frontend/src/App.vue`
  - `frontend/src/stores/`
  - `frontend/src/router/`
  - `frontend/src/api/`
  - `backend/migrations/`
  - `schema.sql`
  - `deploy/`
  - `.github/workflows/`
- 安全控制的直接调用者、共享 helper、测试门禁和部署文件即使不在上述列表中，也不能因路径未列入 CODEOWNERS 而被视为非安全范围。

## 可报告问题与严重度语境

当源码证据表明现实攻击者可以跨越上述边界并产生安全影响时，应报告该问题。典型可报告问题包括但不限于：

- 认证绕过、会话接管、角色提升、跨部门/跨应用/跨资源访问；
- API Key、JWT、LDAP、厂商、数据库、Redis、回调或邮件凭据泄露或跨作用域使用；
- 绕过审批、配额、频控、黑名单、类别策略或真实联调限制；
- 重复发送、错误重试、幂等冲突失效、`uncertain` 状态被自动重发等不可逆副作用；
- 手机号、短信正文、OTP、上行回复或原始厂商载荷以明文或错误上下文持久化、导出、记录或披露；
- 审计缺失、可篡改、不可关联，或在审计失败后仍完成敏感操作；
- 回调、LDAP、厂商或告警路径中的 SSRF、DNS 重绑定、重定向绕过、凭据目标扩张或资源耗尽；
- 导入/导出文件的路径穿越、截断、重排、跨对象拼接、公式注入或明文临时文件；
- 可信代理、TLS、origin、Cookie 或客户端 IP 处理错误导致安全控制退化；
- GitHub Actions、发布门禁、构建产物或公开历史被利用以泄露 Secret、植入代码或绕过审查。

严重度必须根据实际攻击者、权限、暴露面、前置条件、影响范围、成功概率和现有缓解措施校准，而不能只按 CWE 或危险函数名称判断：

- **Critical**：可现实触发的未认证或低权限远程代码执行、广泛核心 Secret 失陷、无限制批量短信副作用、全量敏感数据或平台级控制权失陷。
- **High**：认证/授权绕过、显著权限提升、跨部门或跨应用数据访问、厂商或 LDAP 凭据外泄、可造成真实重复/未授权短信、广泛手机号泄露、审计或发布链核心完整性失陷。
- **Medium**：需要受限权限、非默认配置或多个前置条件，但仍能跨越受支持边界并造成局部敏感数据、完整性、可用性或控制退化。
- **Low**：影响有限、利用概率低或属于有证据支持的纵深防御缺口。没有现实攻击者、边界跨越或安全影响的问题不应仅因代码“不够理想”而升级严重度。

管理员路径、内网路径或可选配置应降低可利用性评估，但不能自动抑制问题；管理员仍受 Secret 作用域、审计、出站允许列表、数据保护和不可逆副作用不变量约束。

## 非报告范围与限制

以下内容通常不单独构成安全漏洞，除非能够证明其进入受支持生产路径或破坏上述不变量：

- 只影响攻击者自身且不产生额外权限、数据、跨主体影响或持久副作用的行为；
- 仅存在于明确隔离、不能在生产启用的 Mock、测试夹具、示例或文档中的行为；
- 没有受影响版本、可达调用链或实际构建依赖的纯版本号告警；
- 不涉及安全控制绕过、敏感数据或不可逆副作用的普通容量、性能、界面或可用性问题；
- 只有通用最佳实践建议、没有源码证据和现实安全影响的理论性问题；
- 项目明确不实现的语音、国际短信、多厂商路由、IAM Provider、双活或 Kubernetes 能力本身。

上述范围不是宽泛免责条款。生产误启用 Mock、不安全默认配置、测试能力泄露到生产、管理员能够突破本应固定的部署边界，或“非目标”代码实际可达时，仍应报告。

本仓库不能单独证明自托管环境的防火墙、证书、代理、Secret 文件权限、外部服务身份或网络隔离是否正确。依赖部署事实的报告应明确其前置条件和证据缺口；缺少现网证明会降低置信度，但不会自动否定源码和部署合同已经支持的攻击路径。

## 私下报告漏洞

请使用 GitHub 仓库 `Security` 页面中的 **Report a vulnerability** 私密报告功能。不要创建公开 Issue，也不要在 Pull Request、讨论区、评论、截图或公开日志中披露未修复漏洞。

报告建议包含：

- 受影响的精确提交、版本或发布候选；
- 攻击者身份、已有权限、必要配置和部署前提；
- 可达入口、攻击者可控输入、失效控制和敏感操作；
- 预期行为、实际行为、具体影响和最强反证；
- 最小、可重复且已脱敏的复现步骤或测试；
- 相关仓库相对路径和行号。

不要在报告中包含：

- 真实手机号、短信正文、回调载荷、上行回复、客户数据或原始日志；
- API Key、JWT、厂商凭据、LDAP/数据库/Redis 密码、回调 Secret、邮件 Key、私钥或 Secret 文件内容；
- 可直接访问的测试/生产地址、SSH 信息、内部拓扑或未经授权的第三方目标；
- 对真实厂商、LDAP、客户回调或生产环境执行的未授权测试结果。

测试应优先使用本地 Mock、隔离副本和最小合成数据。对崩溃、重复发送、凭据使用、外部回调或其他可能产生真实副作用的验证，必须事先获得目标明确的授权。

维护者将在私密渠道确认问题、复核影响、协调修复与披露。公开披露时间、CVE 或公告由维护者与报告者根据修复状态另行约定。

## 仓库与事件响应防护

公开仓库应持续启用 GitHub secret scanning、push protection、Dependabot alerts、私密漏洞报告、默认分支保护、必要的 CODEOWNERS 评审以及最小 Actions 权限。来自 fork 的工作流不得取得环境 Secret 或写权限。

如果 Secret、客户数据、私有历史或其他敏感内容已经进入公开远端，应立即停止发布、将仓库设为私有、吊销或轮换相关凭据、隔离受影响 Git 历史，并从经过验证的干净根重新发布。追加一个“删除敏感内容”的提交不能清除已有 Git 对象中的泄露。
